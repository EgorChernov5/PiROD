# PiROD
Лабораторная работа 1.

## Задание
### Параллельные вычисления и пределы масштабирования

Ограничения лабораторной работы:
- Допустимые языки программирования: C/C++, Python.
- Задачи где присутствует элемент случайности seed должны совпадать в рамках сравнительных экспериментов.
- Вариант: Нахождение элементов, сильно отличающихся от среднего значения (> k·σ).

Этапы выполнения:
- Реализовать последовательную версию решения задачи.
- Реализовать параллельную версию решения задачи. Добавить параметр, ограничивающий допустимый уровень параллелизма (максимальное количество доступных ядер/потоков). Объяснить какую часть задачи можно распараллелить, а какую нельзя.
- Продемонстрировать работу двух версий программ на одинаковых входных параметрах. Результаты должны совпадать.
- Провести анализ детерминизма. Продемонстрировать работу параллельной версии на одинаковых параметрах при N запусках, где N > 100.
- Провести эксперименты для различных входных данных на последовательной и параллельной версии программы. Продемонстрировать масштабируемость решения и влияние увеличения/уменьшения количества доступных ресурсов на итоговый результат. Эксперименты должны быть при различных параметра параллелизма: 1, 2, …, MAX(CPU)/2, …, MAX(CPU), MAX(CPU) * 2, MAX(CPU) * 4.
- Замерить время выполнения, ускорение и эффективность при различных параметрах. Построить графики метрик для полученных экспериментов.
- Объяснить особенности параллелизма для выбранного вами языка программирования. Указать узкие места.

Усложненный вариант:
- Реализация на C/C++.

## Решение

1. Последовательная и параллельная версии решения, а также вспомогательная функция для поиска выбросов, объединены в файле [outliers_catchers.py](outliers_catchers.py).

```python
def squential_catch_outliers(values: list[float], k: float) -> list[float]:
    mean_values = sum(values) / len(values)
    std_values = math.sqrt(
        sum((x - mean_values) ** 2 for x in values) / len(values))
    threshold = k*std_values

    with ProcessPoolExecutor(max_workers=1) as executor:
        results = executor.map(find_outliers, [values],
                               repeat(mean_values), repeat(threshold))

    outliers = []
    for part in results:
        outliers.extend(part)

    return outliers
```

Как работает:
- mean, std и threshold считаются один раз,
- затем используется функция `find_outliers` из того же файла [outliers_catchers.py](outliers_catchers.py), которая возвращает выбросы.

```python
def find_outliers(chunk: list[float], mean: float, threshold: float) -> list[float]:
    return [x for x in chunk if abs(x - mean) > threshold]
```

2. Параллельная версия также находится в файле [outliers_catchers.py](outliers_catchers.py). Добавлен параметр `workers`, ограничивающий допустимый уровень параллелизма.

```python
def parallel_catch_outliers(values: list[float], k: float, workers: int = 4) -> list[float]:
    mean_values = sum(values) / len(values)
    std_values = math.sqrt(sum((x - mean_values) ** 2 for x in values) / len(values))
    threshold = k*std_values
    
    chunk_size = max(1, math.ceil(len(values) / workers))
    chunks = [values[i:i + chunk_size] for i in range(0, len(values), chunk_size)]

    with ProcessPoolExecutor(max_workers=workers) as executor:
        results = executor.map(find_outliers, chunks, repeat(mean_values), repeat(threshold))

    outliers = []
    for part in results:
        outliers.extend(part)

    return outliers
```

Как работает:
- mean, std и threshold считаются один раз,
- массив делится на части, и каждая часть обрабатывается в отдельном процессе,
- список значений, среднее и порог передаются функции `find_outliers` из того же файла [outliers_catchers.py](outliers_catchers.py), которая возвращает выбросы.

В этой задаче распараллеливается не все, а только независимая обработка элементов после подготовки общих параметров.

Нельзя напрямую распараллелить без оговорок:
- Вычисление итогового `mean` как готового одного числа.
- Вычисление итогового `std` как готового одного числа.
- Потому что это глобальные характеристики всего массива: они зависят от всех элементов сразу.

Но их можно вычислять поэтапно параллельно:
- Разбить массив на куски.
- Для каждого куска параллельно посчитать частичную сумму.
- Потом объединить частичные суммы и получить `mean`.
- Дальше так же параллельно посчитать частичные суммы квадратов отклонений от `mean`.
- Потом объединить их и получить `std`.

Точно хорошо распараллеливается:
- Проверка каждого элемента на условие `abs(x - mean) > k * std`.
- После того как `mean` и `std` уже известны, каждый элемент проверяется независимо от других.
- Поэтому массив можно разбить на части, и каждый поток/процесс будет искать выбросы только в своем куске.

3. Для демостриции работы алгоритмов в файле []() были реализованы функции `generate_random_floats` - для генерации списка значений и `compare_outliers_catchers` - для вычисления времени работы алгоритмов.

Параметры:
- длина списка: 100.000
- k: 0.2
- кол-во ядер: 4

Результы:
- Sequential time: 0.100873 s
- Parallel time: 0.094917 s
- Speedup: 1.062749
- Efficiency: 0.265687

4. Анализ детерминизма для N = 200 показывает, что в среднем алгоритм отрабатывает за ... .

Параметры:
- n: 200
- длина списка: 100.000
- k: 0.2
- кол-во ядер: 4

![Анализ детерминизма](artifacts/parallel_catch_outliers.png)

5. Эксперименты для различных входных данных выполнялись в файле [catcher_evaluators.py](catcher_evaluators.py) функцией `evaluate_outliers_catchers`, которая последовательно перебирает наборы входных размеров, порогов и числа процессов и сохраняет результаты в [artifacts/compare_logs.json](artifacts/compare_logs.json). Для генерации входных данных использовалась функция `generate_random_floats` с фиксированным `seed = 42`, поэтому последовательная и параллельная версии сравнивались на одних и тех же массивах.

В экспериментах использовались размеры списков `100000`, `400000`, `700000`, `1000000`, `1300000`, пороги `k = 0.2` и `k = 0.7`, а также параметры параллелизма `1, 2, 3, 4, 5, 6, 7, 8, 16, 32`, что соответствует схеме `1, 2, ..., MAX(CPU)/2, ..., MAX(CPU), MAX(CPU) * 2, MAX(CPU) * 4` для `MAX(CPU) = 8`.

По полученным результатам видно, что решение масштабируется слабо и только на части конфигураций. При увеличении размера входа среднее последовательное время закономерно растет: примерно от `0.50` с для `100000` элементов до `0.79` с для `1300000` элементов. Небольшой выигрыш от параллельной версии появляется только на части запусков и в основном при умеренном числе процессов `workers = 2..4`. Лучшие результаты в логах получены для конфигураций `N = 400000, k = 0.7, workers = 2`, где ускорение составило `1.0557`, и `N = 1300000, k = 0.2, workers = 4`, где ускорение составило `1.0552`. При дальнейшем увеличении числа процессов масштабируемость ухудшается: для `workers = 16` среднее ускорение по всем экспериментам снижается до `0.5148`, а для `workers = 32` до `0.2460`, то есть программа начинает работать заметно медленнее последовательной версии. Это показывает, что увеличение числа доступных ресурсов само по себе не гарантирует ускорение, если объем вычислений в каждой задаче слишком мал по сравнению с накладными расходами на распараллеливание.

6. Для анализа метрик в файле [visualizator.py](visualizator.py) реализованы функции `plot_execution_time_vs_input_size`, `plot_speedup_vs_workers`, `plot_efficiency_vs_workers`, `plot_execution_time_vs_workers` и `plot_speedup_heatmap`, а результирующие изображения сохраняются в папку [artifacts](artifacts).

На всех ключевых графиках последовательная версия удерживается как baseline: для времени она показана отдельной линией `Sequential baseline`, для ускорения и эффективности baseline соответствует уровню `1`. График `execution_time_vs_input_size.png` показывает рост абсолютного времени при увеличении числа элементов и позволяет сравнить последовательную и параллельную версии при разных `workers`. График `speedup_vs_workers.png` показывает, что в среднем лучший результат достигается при `workers = 2`, где среднее ускорение по всем конфигурациям равно `1.0132`, после чего выигрыш быстро исчезает. График `efficiency_vs_workers.png` показывает ожидаемое падение эффективности при росте числа процессов: от `0.5066` при `workers = 2` до `0.0322` при `workers = 16` и `0.0077` при `workers = 32`. График `execution_time_vs_workers.png` дополнительно показывает, что после некоторой точки увеличение числа процессов только увеличивает общее время выполнения. Heatmap `speedup_heatmap.png` удобно визуализирует области, где ускорение выше `1`, и подтверждает, что такие области локальны и наблюдаются только при умеренном числе процессов.

## Итог

Особенности параллелизма в выбранном языке программирования Python связаны с тем, что для CPU-bound задач обычные потоки ограничены `GIL`, поэтому в работе используется именно `ProcessPoolExecutor`, создающий отдельные процессы. Это позволяет реально задействовать несколько ядер, но одновременно добавляет существенные накладные расходы: нужно запускать процессы, разбивать входной список на чанки, сериализовывать данные и передавать их между процессами, а затем собирать общий результат. В данной реализации узким местом является то, что список значений сначала целиком формируется в памяти, затем делится на части `chunks = [values[i:i + chunk_size] for i in range(0, len(values), chunk_size)]`, а каждая часть передается в отдельный процесс. Для сравнительно небольших объемов работы проверка `abs(x - mean) > threshold` слишком проста, поэтому накладные расходы на межпроцессное взаимодействие оказываются больше выигрыша от параллельного выполнения. Дополнительным узким местом является то, что вычисление `mean` и `std` в текущей версии выполняется последовательно до запуска параллельной части, то есть часть времени вообще не распараллеливается. Именно поэтому при больших значениях `workers`, особенно `16` и `32`, наблюдается не ускорение, а деградация производительности.
